node_modules
.next
.git
*.md
data
media

# The retired Django tree and its untracked `.venv` -- reference only, never
# part of an image. Without this the build context grows by hundreds of MB.
old

# Reference material and tooling that no build stage reads.
parity
docs
.github
.claude
.superpowers

# NOTE: do not add `drizzle` or `drizzle/meta` here -- the runtime stage copies
# the migrations folder, and its placeholder `meta/_journal.json` must reach the
# build context (see the COPY comment in the Dockerfile).
