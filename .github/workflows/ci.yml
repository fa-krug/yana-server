# The only pipeline. The Django one was moved to `old/ci.yml` in the folder
# swap, where GitHub does not look for workflows, so it no longer runs.
#
# Still no publish or deploy jobs. Production currently runs the last image the
# Django pipeline pushed to `${{ secrets.DOCKERHUB_USERNAME }}/yana`, and this
# app is not yet a replacement for it -- publishing `:latest` from here and
# redeploying the shared Portainer stack would swap a working aggregator for a
# scaffold. The build jobs still build both architectures for real (`push:
# false`) so the Dockerfile keeps getting exercised.
#
# Restore `publish-manifest` and `deploy` (copy them from `old/ci.yml`, whose
# tag rules and Portainer redeploy are the ones production expects) only once
# this app can actually serve production.
name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: package-lock.json

      # Fails on a stale lockfile rather than resolving something untested.
      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Type check
        run: npm run typecheck

      - name: Test
        run: npm test

  # Build AMD64 image (push:false -- see file header).
  build-amd64:
    name: Build AMD64
    runs-on: ubuntu-latest
    needs: [test]
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build AMD64 (no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          cache-from: type=gha,scope=next-amd64
          cache-to: type=gha,mode=max,scope=next-amd64
          platforms: linux/amd64
          provenance: false

  # Build ARM64 image (push:false -- see file header).
  build-arm64:
    name: Build ARM64
    runs-on: ubuntu-24.04-arm
    needs: [test]
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build ARM64 (no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          cache-from: type=gha,scope=next-arm64
          cache-to: type=gha,mode=max,scope=next-arm64
          platforms: linux/arm64
          provenance: false
